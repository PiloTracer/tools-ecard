// Prisma schema for E-Cards PostgreSQL database
// TODO [backend]: Implement full schema based on domain models

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// MOCK: Example model
// Uncomment and expand when ready to implement

// model User {
//   id        String   @id @default(uuid())
//   email     String   @unique
//   name      String
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
//
//   @@map("users")
// }
