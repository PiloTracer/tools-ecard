# Integration cache

Vendor-published API/XSD/PDF/OpenAPI mirrors for this project live here (not under `.ai/`).

Record downloads in `MANIFEST.txt` (source URL, local path, batch date). Never store secrets.
