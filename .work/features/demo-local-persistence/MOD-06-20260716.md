# MOD-06 AI amplification — M4 Demo + prod restore

**Date:** 2026-07-16  
**Iteration:** M4  
**AI-assisted:** yes

## AI change risk summary
- AI-assisted: yes
- Boundaries crossed: 2 — front-cards (Demo adapters / auth) · api-server (write guard)
- New cross-boundary deps: none (client adapters do not call API when Demo; server guard is env-local)
- Test isolation: ok — command: `docker compose -f docker-compose.dev.yml exec -e DEBUG= -e NODE_OPTIONS=--max-old-space-size=4096 api-server sh -c "cd /app && npm test -- --testPathPattern=demoModeGuard --forceExit --coverage=false"` and front-cards `npm test -- --testPathPatterns=isDemoMode --coverage=false` — `measured`
- Human architectural review: optional — reason: ADR 007 + Approved SPEC; dual client/server write ban is explicit
- Blast radius: If Demo adapters fail open, user content could hit API; mitigated by `DEMO_MODE` server guard on dedicated Demo hosts. If guard mis-fires in prod, legitimate writes fail until env fixed. Browser storage quota can break large Demo sessions (Clear Demo Data).

## Recommendation
merge_with_conditions — reason: full suite + lint of Demo paths should stay green; compose services may need restart to pick `DEMO_MODE` env.

## Conditions if merge_with_conditions
- Must keep demoModeGuard + isDemoMode unit tests green
- Must not enable `DEMO_MODE=true` on a host that should persist data
