@x-director -  I need to add a few features that require both backend and front-end (ui) development.  All features are associated with how to make it easier for users to import batches and start using the ecards application.  All features must be fully operational either the application is running as Demo (demo=true) or as Production. In a previous session, the application was modified to properly map all supported vcard field names from a text file, and I think it also supports import of excel files.    Following is a list of tasks for you to complete properly:  1- verify how the import is working, including the import of pasted text, and uploaded text and excel files, 2- verify the system is able to parse pasted ecard batches, 3- verify the app is able to parse uploaded text files, 4- verify the app is able to parse excel files, 5- these verifications must certify that these parsings support some customized field/column names as well as vcard field/column names.  After these verifications we need a very complete plan generated that adds the following (continue with the nummeration): 6- we need a template xlsx file as reference for users to download with the vcard field/column names supported, so that the user only needs to include the data and then import/upload the file to generate the ecards, in this instance, the first row of the excel files will have the headers (vcard field/names), 7- generate a similar excel file but the vcard field names are in the first column (vertical/transposed approach), and the user would have to complete data in the adjacent columns, instead of rows, 8- Additionally, there must exist a mapping feature, that allows the user to define mappings of existing excel/text files (for example excel files generated by 3rd-party app) where the user can pair the original field name from the file with the vcard field name, this may require to upload a file first, probably an in-process additional step on parsing, for example, the parser parses the columns/fields, but if unable to mapp to the target fields in the database, it asks for proper mapping, 9- This must work for uploaded files and directly-pasted data, 10- we need to be able to define templates feature, where a user is able to save a design as a template that can be used later to create a variation out of such template, this would allow the user to open a template, and when saving it will not save as a templaet, but as a card design, unless the user specifies he wants to save it as a new template, these user-specific templates will be available only to such user. 11- But this template feature has a caveat, the software owners (super-users) should be able to create global templates that can be viewed/used globally by all users using the application, these global templates cannot be overwritten by regular users, however regular users can create customized versions available to them.  Global templates must remain unchanged.  Global templates can only be updated/modified by super-users.  -----  You must carefully analyze these new features, propose professional approach to their implementation, and generate great multi-iteration plan to implement everything reliably. if you have any questions ask.


make sure not to cause regression errors
make sure to add as many tests as necessary for the new features
remember to first provide a full plan that can be implemented incrementally through several passes of implementations


