# tools-ecards (E-Cards) — Screen map

**Doc:** UI foundation **04** · **Created:** 2026-08-14 · **Status:** Draft · **Needs:** nothing (routes measured from `front-cards/app/`)

## Screens

| Slug | Route | Priority | Domain SPEC link | SPEC status |
|------|-------|----------|------------------|-------------|
| landing | `/` | P1 | - (minimal auth-aware page; forwards OAuth params to `/login`) | none yet |
| login | `/login` | P0 | `.work/features/from-claude/authentication/` | none yet |
| auth-callback | `/auth/callback` | P0 | authentication (OAuth state + token exchange) | none yet |
| auth-continue | `/auth/continue` | P0 | authentication (same-origin cookie hop) | none yet |
| dashboard | `/dashboard` | P0 | `simple-projects/`, `simple-quick-actions/` | none yet |
| batches | `/batches` | P0 | `batch-view/` (list + filters + pagination) | none yet |
| batch-records | `/batches/[batchId]/records` | P0 | `batch-records/` (list + search + edit modal) | none yet |
| batch-import | (upload/paste + mapping modal, from `/dashboard` + `/batches`) | P0 | `batch-upload/` + `batch-import/` (FieldMappingModal) | none yet |
| template-designer | `/template-textile` | P0 | `template-textile/` (canvas, toolbox, panels, save/open) | none yet |
| profile | `/profile` | P2 | (account read-only + logout) | none yet |
| demo | `/demo` + demo banner overlay (all routes when active) | P0 | `demo-local-persistence/20260716-SPEC.md` + ADR `20260716-007` | SPEC exists |

**Mode note:** Demo is a **mode, not a route tree** — same routes render with browser-only persistence + sticky orange banner (`DemoModeProvider.tsx`); `/demo` sets the flag and redirects to `/dashboard`.

**Role-gated surfaces:** global-template gallery/save/fork visibility differs by `app_roles` claim (`appsuper`/`appglobal`) — capture role-filtered variants in the designer SPEC (§ screen dependencies).

## Milestones (UI)

| Milestone | Screens | Notes |
|-----------|---------|-------|
| S1 | app-shell (all routes), dashboard, batches, batch-records | shared AppShell + Card/Badge/Modal/DataTable primitives; hostile-input states (loading/empty/error) on every route |
| S2 | template-designer (toolbox/panels/toolbar), batch-import mapping modal | designer chrome primitives (RangeSlider, Toggle, Stepper); mapping modal as Modal primitive |
| S3 | login/auth flows, profile, landing polish | form patterns + copy (`@ui-copy write`), a11y audit |

## Dependencies

- Backend APIs: link `.work/features/` SPECs — do not duplicate contracts here (`simple-projects`, `batch-upload`, `batch-parsing`, `batch-records`, `s3-bucket`, `render-worker`, `demo-local-persistence`, `from-claude/authentication`).
- Canvas engine: Fabric.js v6 + Zustand stores (`templateStore`, `canvasStore`) — UI SPECs reference, do not restate.
- i18n: EN/ES messages (`features/i18n/messages/`) — copy deliverables via `@ui-copy`.

## Next action

`@ui-design-foundation certify screen-spec-ready`
